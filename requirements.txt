# PyTorch must come from NVIDIA's CUDA index, not PyPI — the PyPI wheels have no
# aarch64+CUDA build. Install it first, separately:
#
#   pip install --index-url https://download.pytorch.org/whl/cu130 torch torchvision
#
# Verified on this host: torch 2.13.0+cu130 / torchvision 0.28.0+cu130 on a GB10
# (sm_121), which runs on the build's sm_120 kernels.
torch>=2.9
torchvision>=0.24

# --- Everything else ----------------------------------------------------------
numpy>=1.26
pillow>=10.2
matplotlib>=3.8
python-dotenv>=1.0

# Notebook
jupyterlab>=4.1
ipywidgets>=8.1
